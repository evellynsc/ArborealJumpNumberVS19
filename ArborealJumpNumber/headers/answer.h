#pragma once

#include <string>

#include <cmath>
#include <fstream>

struct Answer {
public:
   

    Answer () {
    }

    //friend std::ostream& operator<<(std::ostream& out, const Answer & cur);
    /*
       friend istream &operator>>( istream  &input, Ans &cur ) {
       }
     */
 
};
